#include(подключать) /*Директива препроцессора*/ <stdio.h>(содержит функции для ввода и для вывода) /*Библиотека с набором функций, подключает заголовочные файлы*/
									standart input / output headers
									стандарный ввод(клавиатура) / вывод(экран) заголовочный файл(где находятся определения функций)